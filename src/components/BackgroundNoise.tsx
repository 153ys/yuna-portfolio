const BackgroundNoise = () => (
  <div
    className="fixed inset-0 z-100 pointer-events-none opacity-[0.6] bg-[url('/background_noise.png')] bg-size-[80px_auto] bg-repeat"
    aria-hidden="true"
  />
);

export default BackgroundNoise;
